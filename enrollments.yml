spring:
  application:
    name: Enrollment
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/enrollment
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
